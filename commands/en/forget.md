Delete specified information from Memoriq memory.

Query: $ARGUMENTS

1. First call memory_search with the query so the user can see
   what will be deleted.
2. Display found items with numbers.
3. Ask the user which items to delete (by number).
4. After confirmation call memory_delete with IDs of selected items.
